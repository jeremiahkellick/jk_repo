{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "jk_repo C build",
            "command": "${workspaceFolder}/bin/jk_build ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "clear": true,
            },
            "windows": {
              "command": "${workspaceFolder}/bin/jk_msvc_shell.bat && ${workspaceFolder}/bin/jk_build ${file}"
            }
        }
    ]
}
